#include <catch2/catch_test_macros.hpp>

#include <string>

#include <skymapf/version.hpp>

TEST_CASE("version string contains prerelease suffix", "[version]") {
    // REQUIRE(skymapf::kVersionString[0] != '\0');
    // REQUIRE(std::string(skymapf::kVersionString).find("-beta") != std::string::npos);
}
