[build-system]
requires = [
  "scikit-build-core>=0.10",
  "pybind11>=2.13",
]
build-backend = "scikit_build_core.build"

[project]
name = "skymapf"
version = "0.0.1b0"
description = "Protocol SDK and optional native bindings for SkyMAPF solvers"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "shangwen song" }
]
license = { text = "MIT" }
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: C++",
  "Operating System :: Microsoft :: Windows",
  "Operating System :: POSIX :: Linux",
  "Operating System :: MacOS",
]

[tool.scikit-build]
wheel.packages = ["python/skymapf"]
build-dir = "build/{wheel_tag}"

[tool.scikit-build.cmake.define]
SKYMAPF_INSTALL_CPP_SDK = "OFF"
SKYMAPF_BUILD_CLI = "OFF"
SKYMAPF_BUILD_TESTS = "OFF"
SKYMAPF_BUILD_EXAMPLES = "OFF"

[tool.cibuildwheel]
build = "cp310-* cp311-* cp312-* cp313-*"
skip = "*-musllinux_*"
test-command = "python -c \"import skymapf; print(skymapf.__version__, skymapf.HAS_CORE)\""
